---
import { getTranslations } from '../utils/translations';

const lang = Astro.url.pathname.startsWith('/en') ? 'en' : 'es';
const t = getTranslations(lang);
// Normaliza la ruta actual quitando prefijo /en o /es
const currentPath = Astro.url.pathname.replace(/^\/(en|es)/, '') || '/';
const otherLang = lang === 'es' ? 'en' : 'es';
// Construye la ruta del otro idioma con su prefijo correspondiente
const otherPath = otherLang === 'en' ? `/en${currentPath}` : `/es${currentPath}`;
---

<header class="fixed top-0 left-0 right-0 z-50 bg-black/80 backdrop-blur-md border-b border-gray-800">
  <nav class="container mx-auto px-6 py-4">
    <div class="flex items-center justify-between">
      
      <div class="flex items-center space-x-2">
        <div class="w-10 h-10 rounded-lg overflow-hidden">
          <img src="/logogera.jpg" alt="geradev.com" class="w-full h-full object-cover" />
        </div>
        <span class="text-xl font-bold">geradev.com</span>
      </div>
      
      
      <div class="hidden md:flex items-center space-x-8">
        <a href="#about" class="text-gray-300 hover:text-green-400 transition-colors duration-300 font-medium">
          {t.nav.about}
        </a>
        <a href="#projects" class="text-gray-300 hover:text-green-400 transition-colors duration-300 font-medium">
          {t.nav.projects}
        </a>
        <a href="#clients" class="text-gray-300 hover:text-green-400 transition-colors duration-300 font-medium">
          {t.nav.clients}
        </a>
        <a href="#contact" class="text-gray-300 hover:text-green-400 transition-colors duration-300 font-medium">
          {t.nav.contact}
        </a>
        
        
        <div class="relative group">
          <button class="flex items-center space-x-2 text-gray-300 hover:text-green-400 transition-colors duration-300 font-medium">
            <span class="text-sm">{lang === 'es' ? '🇪🇸' : '🇺🇸'}</span>
            <span class="text-sm">{lang.toUpperCase()}</span>
            <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7"></path>
            </svg>
          </button>
          
          
          <div class="absolute right-0 mt-2 w-32 bg-gray-900 border border-gray-700 rounded-lg shadow-xl opacity-0 invisible group-hover:opacity-100 group-hover:visible transition-all duration-300">
            <a href={otherPath} class="block px-4 py-3 text-white hover:bg-gray-800 rounded-lg transition-colors duration-300">
              <div class="flex items-center space-x-2">
                <span class="text-sm">{otherLang === 'es' ? '🇪🇸' : '🇺🇸'}</span>
                <span class="text-sm">{otherLang === 'es' ? 'Español' : 'English'}</span>
              </div>
            </a>
          </div>
        </div>
        
        
        <div class="relative group">
          <button id="cv-dropdown" class="flex items-center space-x-2 bg-gradient-to-r from-green-400 to-blue-500 text-black px-4 py-2 rounded-lg font-semibold hover:scale-105 transition-transform duration-300">
            <span>{t.nav.downloadCV}</span>
            <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7"></path>
            </svg>
          </button>
          
          
          <div id="cv-menu" class="absolute right-0 mt-2 w-48 bg-gray-900 border border-gray-700 rounded-lg shadow-xl opacity-0 invisible group-hover:opacity-100 group-hover:visible transition-all duration-300">
            <a href="/CV-ESPAÑOL.pdf" download class="block px-4 py-3 text-white hover:bg-gray-800 rounded-t-lg transition-colors duration-300">
              <div class="flex items-center space-x-2">
                <span class="text-sm">🇪🇸</span>
                <span>{lang === 'es' ? 'CV en Español' : 'CV in Spanish'}</span>
              </div>
            </a>
            <a href="/CV-INGLES.pdf" download class="block px-4 py-3 text-white hover:bg-gray-800 rounded-b-lg transition-colors duration-300">
              <div class="flex items-center space-x-2">
                <span class="text-sm">🇺🇸</span>
                <span>{lang === 'es' ? 'CV en Inglés' : 'CV in English'}</span>
              </div>
            </a>
          </div>
        </div>
      </div>
      
      
      <button id="mobile-menu-btn" class="md:hidden text-white">
        <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16"></path>
        </svg>
      </button>
    </div>
    
    
    <div id="mobile-menu" class="md:hidden mt-4 pb-4 border-t border-gray-800 pt-4 hidden">
      <div class="flex flex-col space-y-4">
        <a href="#about" class="text-gray-300 hover:text-green-400 transition-colors duration-300 font-medium">
          {t.nav.about}
        </a>
        <a href="#projects" class="text-gray-300 hover:text-green-400 transition-colors duration-300 font-medium">
          {t.nav.projects}
        </a>
        <a href="#clients" class="text-gray-300 hover:text-green-400 transition-colors duration-300 font-medium">
          {t.nav.clients}
        </a>
        <a href="#contact" class="text-gray-300 hover:text-green-400 transition-colors duration-300 font-medium">
          {t.nav.contact}
        </a>
        
        
        <div class="pt-4 border-t border-gray-800">
          <p class="text-sm text-gray-400 mb-2">Idioma / Language:</p>
          <div class="flex space-x-2">
            <a href={`/es${currentPath}`} class={`flex-1 px-4 py-2 rounded-lg font-semibold text-center transition-all duration-300 ${lang === 'es' ? 'bg-green-400 text-black' : 'bg-gray-800 text-gray-300 hover:bg-gray-700'}`}>
              🇪🇸 Español
            </a>
            <a href={`/en${currentPath}`} class={`flex-1 px-4 py-2 rounded-lg font-semibold text-center transition-all duration-300 ${lang === 'en' ? 'bg-green-400 text-black' : 'bg-gray-800 text-gray-300 hover:bg-gray-700'}`}>
              🇺🇸 English
            </a>
          </div>
        </div>
        
        
        <div class="pt-4 border-t border-gray-800">
          <p class="text-sm text-gray-400 mb-2">{lang === 'es' ? 'Descargar CV:' : 'Download CV:'}</p>
          <div class="flex space-x-2">
            <a href="/CV-ESPAÑOL.pdf" download class="flex-1 bg-gradient-to-r from-green-400 to-blue-500 text-black px-4 py-2 rounded-lg font-semibold text-center hover:scale-105 transition-transform duration-300">
              🇪🇸 {lang === 'es' ? 'Español' : 'Spanish'}
            </a>
            <a href="/CV-INGLES.pdf" download class="flex-1 bg-gradient-to-r from-green-400 to-blue-500 text-black px-4 py-2 rounded-lg font-semibold text-center hover:scale-105 transition-transform duration-300">
              🇺🇸 {lang === 'es' ? 'Inglés' : 'English'}
            </a>
          </div>
        </div>
      </div>
    </div>
  </nav>
</header>

<script>
  // Mobile menu toggle
  const mobileMenuBtn = document.getElementById('mobile-menu-btn');
  const mobileMenu = document.getElementById('mobile-menu');
  
  mobileMenuBtn?.addEventListener('click', () => {
    mobileMenu?.classList.toggle('hidden');
  });
  
  // Smooth scrolling for navigation links
  document.querySelectorAll('a[href^="#"]').forEach(anchor => {
    anchor.addEventListener('click', function (e) {
      e.preventDefault();
      const target = document.querySelector(this.getAttribute('href'));
      if (target) {
        target.scrollIntoView({
          behavior: 'smooth',
          block: 'start'
        });
        // Close mobile menu if open
        mobileMenu?.classList.add('hidden');
      }
    });
  });
  
  // Header background on scroll
  window.addEventListener('scroll', () => {
    const header = document.querySelector('header');
    if (window.scrollY > 100) {
      header?.classList.add('bg-black/95');
    } else {
      header?.classList.remove('bg-black/95');
    }
  });
</script>
